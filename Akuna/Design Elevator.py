'''
I think we should have the following classes and interfaces:

ElevatorCar(class)
ElevatorController(class)
Button(Interface)
 - FloorButton(class): Buttons on each floor, control the direction
 - CarButton(class): Buttons in the elevator car, control the destinations and open/close the door, report emergency
Panel(Interface)
 - DirectionPanel(class): show the direction of elevator
 - PositionPanel(class): show the position of elevator
'''
